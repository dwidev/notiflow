name: notiflow_example
description: "Example app demonstrating NotiFlow — Notification Runtime Framework for Flutter."
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  notiflow:
    path: ../../packages/notiflow

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
